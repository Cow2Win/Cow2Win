package org.c2w.data.model;

/**
 * The 7 combat roles of heroes in the Hero Wars web/browser version (Dominion Era).
 * Some heroes have two roles at once (e.g. Cleaver = TANK + CONTROL).
 */
public enum Role {
    TANK,
    WARRIOR,
    MARKSMAN,
    MAGE,
    SUPPORT,
    CONTROL,
    HEALER
}
